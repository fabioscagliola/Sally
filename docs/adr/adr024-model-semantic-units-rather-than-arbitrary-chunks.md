# ADR024 — Model semantic units rather than arbitrary chunks

Status: Accepted

## Context

Arbitrary text or token chunks lose much of the structure that makes software relationships useful to agents.

## Decision

The graph represents meaningful software and documentation concepts and their relationships rather than arbitrary token-sized chunks.
