# ADR025 — Use Neo4j as the initial graph store

Status: Accepted

## Context

Sally needs a graph database that can represent and query semantic relationships without introducing multiple persistence technologies prematurely.

## Decision

Neo4j is the initial and only graph store until concrete requirements justify an additional persistence technology.
