{
  "name": "sally-graph-rag-typescript-ingestion",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "ingest": "npm run build && node dist/src/cli.js"
  },
  "dependencies": {
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
